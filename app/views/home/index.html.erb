
<div class="welcome-handler">
  <div class="ui inverted vertical masthead center aligned segment circular" id="welcome-segment">
    <div class="ui text container">
      <h1 class="ui inverted header">
        Financial supervision
      </h1>
      <h2>Keep track of your funds in a convenient way</h2>
      <% if user_signed_in? %>
        <%=link_to 'Get Started ', transactions_path, class: 'ui huge primary button ' %>
      <%else %>
        <%=link_to 'Sign Up ',new_user_registration_path , class: 'ui huge primary button ' %>
    <%end %>
    </div>
  </div>
</div>